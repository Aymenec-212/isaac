# Build using a local tag first; switch to an immutable registry digest for deployment.
ASR_IMAGE=mosaique-asr:r1-build
ASR_PRIVATE_IP=10.20.2.4
MODEL_DIR=/srv/mosaique/models
ASR_KEY_FILE=/etc/mosaique/asr_api_key
